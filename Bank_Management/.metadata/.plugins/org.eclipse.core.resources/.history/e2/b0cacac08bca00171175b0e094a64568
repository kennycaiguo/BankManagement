import cx_Oracle
conn=cx_Oracle.connect("Bank/12345678")
cur=conn.cursor()
print(conn.version)
"""cur.execute("create table customer (acc_no varchar2(10) primary key,name varchar2(20), phone_no number,aadhaar number,e-mail varchar2(40),address varchar2(50),cust_type varchar2(10)")
cur.execute("create table balance (acc_no varchar2(10) primary key, bal number)")
cur.execute("create table tranx (acc_no varchar2(10) primary key,tranx_date date,amount number,type varchar2(2))")
con.commit()
con.close()"""
